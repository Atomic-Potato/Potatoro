extends FileDialog


# Called when the node enters the scene tree for the first time.
#func _ready():
	#root_subfolder = ProjectSettings.globalize_path("user://")
	#current_dir = ProjectSettings.globalize_path("user://")
	#OS.shell_open(ProjectSettings.globalize_path("user://"))
